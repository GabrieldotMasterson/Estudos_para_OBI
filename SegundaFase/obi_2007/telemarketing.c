#include <stdio.h>
#include <stdlib.h>

int main() {

    int qtd;
    int n_lig;
    scanf("%d %d", &qtd, &n_lig);
    int *ligacoes = calloc(4, sizeof(double));

    ligacoes = realloc(ligacoes, n_lig*sizeof(int));

    

    


}